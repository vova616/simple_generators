[package]
name = "macros"
version = "0.1.0"
authors = ["Vova <vova616@gmail.com>"]
edition = "2018"

[lib]
proc-macro = true

[dependencies]
syn = {version = "0.15.26", features = ["full"]}
quote = "0.6.11"
adapter = { path = "../adapter", version = "*" }
